<!DOCTYPE html>
<html lang="en">
  <head>
    {{template "head" .}}
  </head>
  <body>
    <div class="wrapper">
      <div class="main-panel" style="width: 100% !important;">
        {{template "navbar" .}}
        {{template "loadingscreen" .}}
        {{template "content" .}}
      </div>
    </div>
  </body>
</html>
