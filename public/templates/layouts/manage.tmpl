<!DOCTYPE html>
<html lang="">
  <head>
    {{template "head" .}}
  </head>
  <body>
    <div class="wrapper">
      <div class="main-panel" style="width: 100% !important;">
        {{template "navbar" .}}
        {{template "content" .}}
      </div>
    </div>
  </body>
</html>
